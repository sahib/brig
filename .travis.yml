language: go
sudo: required
go:
    - 1.10
notifications:
    email:
      - sahib@online.de
install:
    - sudo apt-get install fuse
    - go get -t -v github.com/sahib/brig
    - cd $GOPATH/src/github.com/sahib/brig
    - make

script:
    - cd $GOPATH/src/github.com/sahib/brig
    - go test -v ./...

package transfer.protocol;
option go_package = "wire";

enum RequestType {
    INVALID = 0;
    DUMMY = 1;
}

message Request {
	RequestType req_type = 1;
    bytes data = 2;
}

message Response {
	RequestType req_type = 1;
	bytes data = 2;
    int64 ID = 3;
    string error = 4;
}
